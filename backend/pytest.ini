[pytest]
testpaths =
    apps/titanic/tests
    apps/auth/tests
python_files = test_*.py
asyncio_mode = auto
markers =
    integration: 외부 서비스(Ollama 등)가 필요한 통합 테스트
    ollama: 로컬 Ollama + EEVE-Korean 모델 필요
addopts = -m "not integration"
